import ClassFieldTheory.KroneckerWeber.Core

set_option autoImplicit false

/-!
# Proof of the global Kronecker--Weber theorem

The imported module supplies KroneckerWeber.exists_cyclotomicEmbedding directly.
Comparator selects that existing proved declaration from this environment.
There is no renamed theorem, wrapper proof, reconstructed class instance,
or additional hypothesis here.

Do not import the Challenge module: it deliberately declares the same target
name in a separate statement environment.
-/
